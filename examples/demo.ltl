// examples/demo.ltl — runnable demo for ltl_wireworld

import std::io
use ltl_wireworld as lib

fn main() {
    io::println("ltl_wireworld demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}